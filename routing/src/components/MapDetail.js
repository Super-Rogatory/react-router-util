import React from "react";

function MapDetail() {
  // No API request can be made for a single Map. Not interested in doing all of the extra work to make it work.
  return (
    <div>
      <h1>Specifc Item</h1>
    </div>
  );
}

export default MapDetail;
